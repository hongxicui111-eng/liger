defaults:
  - _self_
  - method: base
  - dataset: amazon
  - logging: wandb

experiment_id: test
seed: 0
device_id: 0
output_path: None
test_method: tiger
# If True, always retrain SASRec + RQ-VAE from scratch.
# Weights are saved under this run's output_path (not the shared cache),
# so old caches are never hit.
force_rerun: False
